#!/bin/env kosh
# The runtime option is the contract under test. Analysis of the whole file
# finishes before the option is set, so the static finding is disabled here.
# shellcheck disable=SC2154
set -WWW
echo "first=$never_a"
set -o no-diagnostics
echo "second=$never_b"
echo done
